# datasets schema information

datasets.path = "/Users/visenger/research/datasets"

column {
  unique = ["RecId"]
}

lookup.columns = [
  {
    name = "City"
    source = ${datasets.path}"/lookups/city.tsv"
  }
  {
    name = "State"
    source = ${datasets.path}"/lookups/state.tsv"
  }
  {
    name = "ZIP"
    source = ${datasets.path}"/lookups/zip.tsv"
  }
]